pre-commit:
  parallel: true
  commands:
    fmt:
      glob: "*.rs"
      run: cargo xtask fmt
    clippy:
      glob: "*.rs"
      run: cargo xtask clippy

pre-push:
  parallel: true
  commands:
    test:
      glob: "*.rs"
      run: cargo xtask test
